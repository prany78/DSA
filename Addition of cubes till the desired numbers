// User function Template for Java

class Solution {
    int sumOfSeries(int n) {
        int sum=0;
        for(int i=0;i<=n;i++)
        {
            sum=sum+(i*i*i);
        }
        return sum;
    }
}
